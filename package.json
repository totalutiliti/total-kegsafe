{
  "private": true,
  "name": "kegsafe",
  "description": "KegSafe - Multi-tenant SaaS keg management system",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  },
  "lint-staged": {
    "backend/src/**/*.ts": "npx --prefix backend eslint --config backend/eslint.config.mjs --no-warn-ignored --fix"
  }
}
